[project]
name = "mac-photos-duplicate-finder"
version = "0.3.0"
description = "Safe tools to inventory macOS Photos libraries, find duplicates, rank copies, and create review albums."
requires-python = ">=3.8"
dependencies = []
license = { file = "LICENSE" }

[tool.ruff]
line-length = 100
